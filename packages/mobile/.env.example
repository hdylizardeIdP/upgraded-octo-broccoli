# API Configuration for React Native Mobile App
# For iOS Simulator: use localhost
# For Android Emulator: use 10.0.2.2 (mapped to host machine's localhost)
# For physical devices: use your machine's local IP address (e.g., 192.168.1.100)
API_URL=http://localhost:3000/api/v1
